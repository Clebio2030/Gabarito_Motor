/* 
   GABARITO - Dummy SQL
   Este script serve apenas para satisfazer o updater antigo (v1.4) 
   e evitar rollback de arquivos.
   As views reais sao aplicadas pelo proprio motor (backend) no boot.
*/

SELECT 'Views criadas' AS RESULTADO FROM RDB$DATABASE;
